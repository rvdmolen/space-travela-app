/**
 * Enum for key definition values.
 * @readonly
 * @enum {string}
 */
const EventDefinitions = Object.freeze({
  TAB_SELECTED: 'tab-selected',
  DOT_SELECTED: 'dot-selected'
});

export { EventDefinitions };
